<!DOCTYPE html>
<html lang="fr">
  <head>
    <title><%= content_for(:title) || "Bissowebsite Admin" %></title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>

    <%= yield :head %>

    <!-- Bootstrap CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    ></script>

    <link rel="icon" href="/icon.png" type="image/png" />
    <link rel="apple-touch-icon" href="/icon.png" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-light text-dark">

    <div class="d-flex min-vh-100">

      <!-- Sidebar -->
      <aside class="d-none d-md-block bg-white shadow-sm border-end" style="width: 250px;">
        <div class="py-4 text-center border-bottom">
          <h2 class="text-primary">Bissowebsite</h2>
        </div>
        <nav class="p-3">
          <ul class="nav flex-column gap-2">
            <li class="nav-item">
              <a href="/admin_panel" class="nav-link text-primary fw-bold">Dashboard</a>
            </li>
            <li class="nav-item">
              <a href="/products" class="nav-link">Produits</a>
            </li>
            <li class="nav-item">
              <a href="/categories" class="nav-link">Catégories</a>
            </li>
            <li class="nav-item">
              <a href="/orders" class="nav-link">Commandes</a>
            </li>
            <li class="nav-item">
              <a href="/logout" class="nav-link text-danger">Déconnexion</a>
            </li>
          </ul>
        </nav>
      </aside>

      <!-- Content area -->
      <div class="flex-grow-1 d-flex flex-column">

        <!-- Header -->
        <header class="bg-white border-bottom px-4 py-3 d-flex justify-content-between align-items-center">
          <h1 class="h5 mb-0"><%= content_for(:page_title) || "Tableau de bord" %></h1>
          <span class="text-muted small">Admin connecté</span>
        </header>

        <!-- Main Content -->
        <main class="flex-grow-1 px-4 py-4">
          <% if notice %>
            <div class="alert alert-success">
              <%= notice %>
            </div>
          <% end %>

          <% if alert %>
            <div class="alert alert-danger">
              <%= alert %>
            </div>
          <% end %>

          <%= yield %>
        </main>

        <!-- Footer -->
        <footer class="bg-white border-top text-center py-3 text-muted small">
          &copy; <%= Time.current.year %> Bissowebsite — Interface admin.
        </footer>

      </div>
    </div>
  </body>
</html>
